export interface IVideosFilters {
  fromDate?: Date;
  toDate?: Date;
  statuses?: number[];
  page?: number;
  limit?: number;
  // lat?: number;
  // lng?: number;
  // radius?: number;
  // eventId?: boolean;
  // id?: number;
}

// tags?: string[];
// tagsJoined: string;
